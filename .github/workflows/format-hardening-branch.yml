# Temporary branch-only maintenance; final test additions, then remove this file.
name: Format hardening branch
on:
  push:
    branches: [fix/proxy-core-hardening]
    paths: [.github/workflows/format-hardening-branch.yml]
permissions:
  contents: write
jobs:
  format:
    if: github.event_name == 'push' && github.ref == 'refs/heads/fix/proxy-core-hardening'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/proxy-core-hardening
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Format only Rust sources and retain the application lockfile
        run: |
          cargo fmt --all
          git add src benches tests
          if git diff --cached --quiet; then exit 0; fi
          git -c user.name='github-actions[bot]' -c user.email='41898282+github-actions[bot]@users.noreply.github.com' commit -m 'style: format additional tunnel regression tests'
          git push origin HEAD:refs/heads/fix/proxy-core-hardening
